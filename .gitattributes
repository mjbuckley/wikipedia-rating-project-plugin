# Auto detect text files and perform LF normalization

* text=auto


# Make sure graphics files are treated appropriately (binary expect for .svg)

*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.svg text


# Exclude files not needed for running the plugin from the export zip

/.gitattributes export-ignore
/.gitignore     export-ignore